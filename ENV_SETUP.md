# 🔐 Настройка переменных окружения (.env)

## Быстрая настройка

### Вариант 1: Использовать существующий .env (уже настроен ✓)

Файл `.env` уже создан с вашим API ключом Alem AI. Просто запустите:

```bash
python analytics_agent_alem.py
```

### Вариант 2: Создать свой .env

1. Скопируйте шаблон:
```bash
cp .env.example .env
```

2. Отредактируйте `.env`:
```bash
nano .env
# или
code .env
```

3. Добавьте свои ключи:
```env
ALEM_API_KEY=ваш-ключ-здесь
ALEM_MODEL=gpt-4o
```

## 📋 Структура .env файла

```env
# Alem AI API Configuration
ALEM_API_KEY=sk-BYXPF09hIOPGYsKGeswMoQ
ALEM_MODEL=gpt-4o

# Anthropic API Configuration (optional)
# ANTHROPIC_API_KEY=your-anthropic-key-here
```

## 🔒 Безопасность

✅ **Файл `.env` добавлен в `.gitignore`** - не попадет в git
✅ **Используйте `.env.example`** для шаблона (без реальных ключей)
✅ **Никогда не коммитьте `.env`** в репозиторий

## 🚀 Использование

### Python автоматически загружает .env:

```python
from dotenv import load_dotenv
import os

load_dotenv()  # Загружает .env

api_key = os.getenv("ALEM_API_KEY")
model = os.getenv("ALEM_MODEL", "gpt-4o")  # с дефолтом
```

### Агенты уже настроены:

Оба агента автоматически загружают `.env` при запуске:

```bash
# Alem AI агент
python analytics_agent_alem.py

# Anthropic агент
python analytics_agent.py
```

## 🔧 Переопределение через командную строку

Можно переопределить значения из `.env`:

```bash
# Временно использовать другую модель
ALEM_MODEL=gpt-4o-mini python analytics_agent_alem.py

# Временно использовать другой ключ
ALEM_API_KEY=другой-ключ python analytics_agent_alem.py
```

## 📝 Доступные переменные

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `ALEM_API_KEY` | API ключ Alem AI | (обязательно) |
| `ALEM_MODEL` | Модель для Alem AI | `gpt-4o` |
| `ANTHROPIC_API_KEY` | API ключ Anthropic | (опционально) |

## 🎯 Доступные модели Alem AI

- `gpt-4o` - самая мощная модель OpenAI (по умолчанию)
- `gpt-4o-mini` - быстрая и дешевая версия
- `claude-3-5-sonnet-20241022` - модель Anthropic

## ⚠️ Troubleshooting

### Ошибка: "API key not found"

```bash
# Проверьте .env файл
cat .env

# Убедитесь что файл существует
ls -la .env

# Проверьте загрузку
python -c "from dotenv import load_dotenv; import os; load_dotenv(); print(os.getenv('ALEM_API_KEY'))"
```

### Ошибка: "Invalid API key"

```bash
# Проверьте правильность ключа в .env
cat .env | grep ALEM_API_KEY

# Убедитесь что нет лишних пробелов
# Правильно: ALEM_API_KEY=sk-xxx
# Неправильно: ALEM_API_KEY = sk-xxx
```

### .env не загружается

```bash
# Убедитесь что python-dotenv установлен
pip list | grep python-dotenv

# Установите если нужно
pip install python-dotenv
```

## 📚 Дополнительная информация

- [python-dotenv документация](https://github.com/theskumar/python-dotenv)
- [12-Factor App: Config](https://12factor.net/config)

## ✅ Проверка настройки

Запустите тест:

```bash
python -c "
from dotenv import load_dotenv
import os

load_dotenv()

print('✅ Проверка .env:')
print(f'ALEM_API_KEY: {\"✓\" if os.getenv(\"ALEM_API_KEY\") else \"✗\"}')
print(f'ALEM_MODEL: {os.getenv(\"ALEM_MODEL\", \"не установлено\")}')
"
```

Ожидаемый результат:
```
✅ Проверка .env:
ALEM_API_KEY: ✓
ALEM_MODEL: gpt-4o
```
